worker_processes 2;

events {
	worker_connections 128;
}

http {
	gzip on;

	server {

		listen 80;
		root /usr/share/writ;

		location /list {
			proxy_pass http://writ/list;
		}
		location /f/ {
			rewrite ^/f/(.*) /$1  break;
			proxy_pass http://writ/$uri;
		}
	}
}